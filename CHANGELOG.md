# Changelog

このプロジェクトの変更履歴。形式は [Keep a Changelog](https://keepachangelog.com/ja/1.1.0/) に基づきます。

## [Unreleased]

### Added
- コース select（自動 / 手動 / おまかせ / 節電 / 花粉 / のどはだ / サーキュ）
- 除湿タンク満水バイナリセンサー（`water_full` フラグ。挙動未検証の注記付き）
- ダッシュボードカード例（`examples/daikin_mcz70_card.yaml`）

### Changed
- FW 3.15.0 対応: 運転モードを `acOpeMode`（0=空気清浄 / 1=除湿 / 2=加湿）で制御（旧 `humd=5` による除湿指定は廃止）、風量ラベルを実機検証値（自動 / しずか / 弱 / 標準 / 高 / ターボ）に修正
- 「給水が必要」バイナリセンサーを「加湿タンク給水」にリネーム（実体を明確化）
- 風量・湿度 select を全モードで操作可能に（フルパラメータ送信で反映・実機検証済み）
- 書き込みは常にフルパラメータ送信（`pow` + `mode` + `airvol` + `humd`）に統一

### Fixed
- トークン永続化で `async_update_entry` を await していた問題（HA 2026.1+ では同期 `@callback` 関数のため `'bool' object can't be awaited` になる）
- LED「明」が効かない問題（`led_dsp=0` はサーバーが無視するため「明」=`-` に修正・実機検証済み）

## [0.1.0] - 2026-08-13

### Added
- 初回リリース: MCZ704A（うるるとさらら空気清浄機）対応の Home Assistant カスタムコンポーネント
- 読み取り: ローカル API（認証なし・2 秒ポーリング。室温 / 湿度 / PM2.5 / ほこり / におい / タンク状態）
- 書き込み: クラウド API（電源 / 運転モード / コース / 風量 / 湿度モード / LED。この機種はローカル書き込みが無効のため）
- トークン自動管理（refresh_token の永続化・期限前自動リフレッシュ・401 時自動リフレッシュ）
- セットアップ簡易化（`scripts/get_credentials.py` による認証情報取得、APK 埋め込み公開定数の自動入力）

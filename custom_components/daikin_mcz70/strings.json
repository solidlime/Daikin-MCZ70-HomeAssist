{
  "config": {
    "step": {
      "user": {
        "title": "Daikin MCZ70 Air Purifier",
        "description": "Enter the device IP address and Daikin Smart DB cloud API credentials. Cloud credentials are optional but required for writes (local writes are disabled on this device).",
        "data": {
          "ip_address": "IP Address",
          "client_id": "Client ID (auto-filled, change only if needed)",
          "uuid": "UUID (auto-filled, change only if needed)",
          "client_secret": "Client Secret (auto-filled, change only if needed)",
          "refresh_token": "Refresh Token (optional)",
          "terminal_id": "Terminal ID (may be left empty)",
          "port": "Port",
          "id": "Device ID",
          "spw": "Device password",
          "redirect_uri": "Redirect URI (auto-filled, change only if needed)",
          "apw": "APW (optional)"
        }
      },
      "cloud_warning": {
        "title": "Cloud login failed",
        "description": "Local connection succeeded, but the Daikin Smart DB cloud login failed. Writes (power, fan speed, humidity mode, LED) will not work until the cloud credentials are fixed. You can re-enter them later via the integration options."
      }
    },
    "abort": {
      "already_configured": "This device is already configured."
    },
    "error": {
      "cannot_connect": "Cannot connect to the device at this IP address."
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "Daikin MCZ70 Air Purifier",
        "description": "Changes are applied after a restart.",
        "data": {
          "ip_address": "IP Address",
          "client_id": "Client ID (auto-filled, change only if needed)",
          "uuid": "UUID (auto-filled, change only if needed)",
          "client_secret": "Client Secret (auto-filled, change only if needed)",
          "refresh_token": "Refresh Token (optional)",
          "terminal_id": "Terminal ID (may be left empty)",
          "port": "Port",
          "id": "Device ID",
          "spw": "Device password",
          "redirect_uri": "Redirect URI (auto-filled, change only if needed)",
          "apw": "APW (optional)"
        }
      }
    }
  },
  "entity": {
    "fan": {
      "fan": { "name": "Air Purifier" }
    },
    "sensor": {
      "temperature": { "name": "Temperature" },
      "humidity":    { "name": "Humidity" },
      "dust":        { "name": "Dust" },
      "odor":        { "name": "Odor" }
    },
    "select": {
      "fan_speed": { "name": "Fan Speed" },
      "operation_mode": {
        "name": "Operation Mode",
        "options": {
          "空気清浄": "Air Purification",
          "除湿空気清浄": "Dehumidifying + Air Purification",
          "加湿空気清浄": "Humidifying + Air Purification"
        }
      },
      "humidity_mode": {
        "name": "Humidity Mode",
        "options": {
          "低め": "Low",
          "標準": "Standard",
          "高め": "High",
          "連続": "Continuous"
        }
      },
      "led": {
        "name": "LED Display",
        "options": {
          "明": "Bright",
          "暗め": "Dim",
          "消灯": "Off"
        }
      }
    },
    "binary_sensor": {
      "water_supply": { "name": "Water Supply" },
      "humd_tank":    { "name": "Humidifier Tank Removed" }
    }
  }
}
